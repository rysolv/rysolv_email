const fundedAccount = require('./fundedAccount');
const fundedIssue = require('./fundedIssue');
const issueResolved = require('./issueResolved');

module.exports = {
  fundedAccount,
  fundedIssue,
  issueResolved,
};
